@{
    ViewData["Title"] = "Acesso Negado";
    Layout = "_Layout";
}

<div class="d-flex flex-column align-items-center justify-content-center" style="min-height: 60vh;">
    <div class="text-center">
        <h2 class="fw-bold text-danger mb-2">Acesso Negado</h2>
        <p class="text-secondary mb-4">Você não tem permissão para acessar esta página.</p>
        <a asp-controller="Account" asp-action="Login" class="btn btn-primary">Voltar ao Login</a>
    </div>
</div>
